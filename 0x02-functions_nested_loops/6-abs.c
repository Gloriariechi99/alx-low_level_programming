/**
 * _abs - function that computes the absolute value of an integer
 * @m: integer whose aboulte value is to be computed
 * Return: Always zero
 */
#include "main.h"
int _abs(int m)
{
	if (m >= 0)
		return (m);
	else
		return (-m);
}
